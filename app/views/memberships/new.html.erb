<h4> Join a Group </h4>

<%= form_for [@user,@membership] do |f| %>
  <div class="input-field col s12">
    <%= f.collection_select :group_id, @group_list, :id, :name%>
  </div>

  <%= f.submit "Join Group" %>
<% end %>